spring:
  datasource:
    url: jdbc:tc:postgresql:18:///auth_service_test
    username: test
    password: test
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

app:
  jwt:
    secret: ${JWT_SECRET:Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24tdGhpcy1pcy1hLTI1Ni1iaXQtc2VjcmV0LWtleS1mb3ItZGV2LW9ubHktY2hhbmdlLWl0}
    issuer: ${JWT_ISSUER:auth-service-test}
    access-token-expiry: ${JWT_ACCESS_TOKEN_EXPIRY:PT15M}
    refresh-token-expiry: ${JWT_REFRESH_TOKEN_EXPIRY:P7D}
    blacklist-key-prefix: ${JWT_BLACKLIST_KEY_PREFIX:auth:jwt:blacklist:test:}
